// Example of a function which allows the user to change the web page to dark mode, which changes all of the 
// elements to allow for a dark background
function toggleDarkMode() {
    var element = document.body; 
    element.classList.toggle("darkMode"); 
}